# Memory — The Pearson Co. content engine

Durable facts and current state. Update the Current State block in every content commit.

## Current state
- 2026-07-23: 120 posts live (next_index = 434). This run published seq 431-433: contract-automation (guide; Forrester/DocuSign 2024 TEI study cited; up-linked to the pillar only, since no money page is live yet in the crm-automation cluster), crm-dashboard-examples (listicle; Salesforce 2026 and HubSpot 2025 State of Sales reports cited; up-linked to the pillar only, since no money page is live yet in the reporting-forecasting cluster), best-crm-for-consultants (comparison; qualitative platform comparison, no external stats needed for this content type; up-linked to the pillar only, since no money page is live yet in the crm-consulting cluster). All passed style gate, link_graph (120 pages, 0 issues), word-count floors with margin. Full detail in this run's BUILD-PLAN.md row and build_state.json notes.
- 2026-07-22: 117 posts live (next_index = 431). This run published seq 428-430: what-is-customer-journey-mapping (definition; up-linked to the pillar only, since no money page is live yet in the customer-journey cluster), market-entry-strategy-framework (guide; HowTo-free but with an entity-structure comparison table; up-links to hospitality-tech-market-entry.html pillar + market-entry-strategy-service.html money, both live), procure-to-pay-process (guide; HowTo schema added; up-linked to the pillar only, since no money page is live yet in the process-sop cluster). All passed style gate, link_graph (117 pages, 0 issues), word-count floors with margin. Full detail in this run's BUILD-PLAN.md row and build_state.json notes.
- 2026-07-21: 114 posts live (next_index = 428). This run published seq 425-427: revenue-operations-team-structure, dynamics-365-implementation-guide (HowTo schema, up-link substitute crm-implementation-consultant.html since dynamics-crm-implementation is not yet live), fractional-cro-for-startups. All passed style gate, link_graph (114 pages, 0 issues), word-count floors with margin. Full detail in the 2026-07-21 BUILD-PLAN.md row and build_state.json history.
- **Date-accuracy audit (closed out, non-issue):** the client's standing note that "articles are all published as June 16th" was investigated in full this run rather than repeated as background. Every file with a literal "16 June 2026" / "2026-06-16" date was found (exactly 21, all site/services/ pillar and money pages from the original launch) and cross-checked against its true git first-commit date: all 21 were genuinely first published on 16 June 2026 in one launch batch, so that date is accurate, not a bug, and must not be rewritten (doing so would falsify the record and risks a GSC date-manipulation flag). The 9 June-dated site/blog/ posts referenced in older logs already carry their own correct 27-29 June dates matching git history. Every new post this routine has ever published has always carried the correct real run date, confirmed again this run (21 July 2026 on all three). Treat this as resolved; if the client raises it again, point to this entry rather than re-auditing from scratch, unless new evidence of an actual live bug appears.
- **Slack link format (verified working as of 2026-07-23):** the 2026-07-22 run wrapped the entire descriptive line, including the "New article live" prefix and the pillar name, inside a single markdown link (`[New article live: Title (Pillar)](url)`), one message per article. This run read back the actual Slack channel history before sending rather than assuming that fix had worked, and confirmed both the 2026-07-22 message and this run's own prior message rendered as clean, separate, correctly terminated `<url|text>` hyperlinks with no bleeding. Continue this exact format on future runs. If the client reports the bleed again after two consecutive runs of confirmed-clean rendering, treat it as a client-side or historical-message issue (an old, pre-fix message still open in their view) rather than re-diagnosing the routine's formatting.
- Phase 1 (scaffold) complete. Repo seeded with the live site; engine in place.
- Phase 2 (the 12-month plan) COMPLETE: 1085 records across 14 pillars (15 pillar pages, 115 money, 955 authority) in `content-plan/plan.json`, rendered to `plan.md` + `plan-rows.js`, reviewable in `content-plan/content-plan.html`. Sequenced for publishing via `scripts/sequence_plan.py` (pillars + money front-loaded, authority round-robin; ~91/month).
- Volumes are live Semrush for batches 1-5 (Conversion, RevOps, CRM Implementation, Fractional+Growth, CJM part 1) and indicative (universe doc + domain knowledge) from batch 6 on. Refresh with Semrush when units are available, then re-run `render_plan.py`.
- 120 posts published: the 15 cornerstone pillar pages (seq 1-15), seq 16-39 (money pages across CRO, CRM, fractional, growth, market entry, ecommerce, RevOps pillars in site/services/), plus 81 authority posts in site/blog/, most recently seq 431-433 (contract-automation, crm-dashboard-examples, best-crm-for-consultants) published 2026-07-23. Full per-run seq history lives in BUILD-PLAN.md's session log. `next_index` = 434, routine resumes at seq 434.
- Netlify git continuous deployment is connected (push to main -> deploy). GA4 (G-3XWNSR5VHF) is live and consent-gated. Sitemap: https://thepearsonco.com/sitemap.xml (124 URLs, submit in GSC).
- 2026-07-20 SEO indexing fix (maintenance run, no posts). GSC flagged 42 URLs as "Discovered, currently not indexed" (Last crawled N/A). Diagnosed as NOT a sitemap fault: robots.txt, sitemap coverage, canonicals and meta robots are all correct, and there is no header-level noindex. Root cause is new-site crawl budget plus an internal-linking gap. The services hub (site/services/index.html) linked only 15 pillar services; added an "Every service, in one place" section linking all 39 service pages, so the long-tail money pages (previously 2-5 inbound links each) now have a strong inbound link from the hub. After deploy, request validation in GSC. The live site is not reachable from the build environment (org network policy blocks thepearsonco.com), so header checks were done from source only.
- The publishing routine (Strategist + 8 souls) is running: 3 posts/run, pointer-based from `build_state.json`. Each new post's publish date is set to the actual run date, not a fixed date; confirmed again this run (23 July 2026 on all three new posts, both in the visible byline and the JSON-LD datePublished/dateModified). Correct on every new post across twenty-one consecutive runs (2-23 July). See the date-accuracy audit bullet above: the June-16 date question is now closed out as a non-issue, not an open backlog item.
- Watch items: (1) the crm-automation pillar (seq 431's cluster), reporting-forecasting pillar (seq 432's cluster) and crm-consulting pillar (seq 433's cluster) all have no live money page yet, only their pillar pages, so all three of today's new posts link up to the pillar only; see build_state.json's notes field for the full current list of not-started money pages per pillar, plus the standing cross-pillar backlog. (2) link_graph.py reports the full graph clean (120 pages, 0 issues) as of this run; this run's three new posts were initially unlinked from elsewhere, fixed proactively before running link_graph.py by adding an inbound Related-reading link from two sibling pages each, so the graph reported clean on the first run. (3) This run's research used live web search (a Forrester Consulting Total Economic Impact study commissioned by DocuSign, 2024; Salesforce's 2026 State of Sales report; HubSpot's 2025 State of Sales report) rather than the Semrush MCP, which was not invoked this run; volumes and KD for seq 431-433 came from the existing plan.json records.

## Fixed decisions
- Repo `Pearson26/pearson-co`; push to `main` via `gh` as `ngindubai` (accepted write collaborator).
- Static HTML, no build step. Netlify publish dir = repo root.
- Articles match the **live site** design (#B784A7, Playfair Display + DM Sans), not the agency-document look (#7f466f + Inter, which is for review documents only).
- Authority articles -> `/blog/<slug>.html`; pillar + money pages -> `/services/<slug>.html`. Root-relative asset paths.
- Byline author is Lauren Pearson, Founder.
- Barbell strategy: small set of money pages fed by a large body of authority content; pillar-and-cluster.
- 14 pillars, RevOps confirmed as a full pillar (umbrella adopted).

## Content rules (see CLAUDE.md for the full list)
- British English. No em dashes, ever. Banned words: delve, meticulous, comprehensive, leverage, seamless, robust (+ extended blocklist).
- Light scheme only. Headings use an italic last phrase via `<em>`.
- Every regulatory/statistical claim is sourced; no invented data or client names.

## Mistakes to avoid
- Do not push to a feature branch; deploy only fires on `main`.
- Do not modify index.html / styles.css / script.js during a content run (sitewide changes are a separate job).
- Do not pick work by date; progression is by pointer (`next_index`).
- Do not enable tracking IDs or wire Netlify without an explicit instruction.
