# Memory — The Pearson Co. content engine

Durable facts and current state. Update the Current State block in every content commit.

## Current state
- Phase 1 (scaffold) complete. Repo seeded with the live site; engine in place.
- Phase 2 (the 12-month plan) COMPLETE: 1085 records across 14 pillars (15 pillar pages, 115 money, 955 authority) in `content-plan/plan.json`, rendered to `plan.md` + `plan-rows.js`, reviewable in `content-plan/content-plan.html`. Sequenced for publishing via `scripts/sequence_plan.py` (pillars + money front-loaded, authority round-robin; ~91/month).
- Volumes are live Semrush for batches 1-5 (Conversion, RevOps, CRM Implementation, Fractional+Growth, CJM part 1) and indicative (universe doc + domain knowledge) from batch 6 on. Refresh with Semrush when units are available, then re-run `render_plan.py`.
- 111 posts published: the 15 cornerstone pillar pages (seq 1-15), seq 16-39 (money pages across CRO, CRM, fractional, growth, market entry, ecommerce, RevOps pillars in site/services/), plus 72 authority posts in site/blog/: seq 145, 201, 355 published 2026-06-27; seq 356-358 published 2026-06-28; seq 359-361 published 2026-06-29; seq 362-364 and seq 365-367 published 2026-07-01; seq 368-370 published 2026-07-02; seq 371-373 published 2026-07-03; seq 374-376 published 2026-07-04; seq 377-379 published 2026-07-05; seq 380-382 published 2026-07-06; seq 383-385 published 2026-07-07; seq 386-388 published 2026-07-08; seq 389-391 published 2026-07-09; seq 392-394 published 2026-07-10; seq 395-397 published 2026-07-11; seq 398-400 published 2026-07-12; seq 401-403 published 2026-07-13; seq 404-406 published 2026-07-14; seq 407-409 published 2026-07-15; seq 410-412 published 2026-07-16; seq 413-415 published 2026-07-17; seq 416-418 published 2026-07-18; seq 419-421 published 2026-07-19; seq 422 (pipeline-hygiene), seq 423 (buyer-personas-b2b), seq 424 (conversion-rate-formula) published 2026-07-20. `next_index` = 425, routine resumes at seq 425.
- Netlify git continuous deployment is connected (push to main -> deploy). GA4 (G-3XWNSR5VHF) is live and consent-gated. Sitemap: https://thepearsonco.com/sitemap.xml (115 URLs, submit in GSC).
- 2026-07-20 SEO indexing fix (maintenance run, no posts). GSC flagged 42 URLs as "Discovered, currently not indexed" (Last crawled N/A). Diagnosed as NOT a sitemap fault: robots.txt, sitemap coverage, canonicals and meta robots are all correct, and there is no header-level noindex. Root cause is new-site crawl budget plus an internal-linking gap. The services hub (site/services/index.html) linked only 15 pillar services; added an "Every service, in one place" section linking all 39 service pages, so the long-tail money pages (previously 2-5 inbound links each) now have a strong inbound link from the hub. After deploy, request validation in GSC. The live site is not reachable from the build environment (org network policy blocks thepearsonco.com), so header checks were done from source only.
- The publishing routine (Strategist + 8 souls) is running: 3 posts/run, pointer-based from `build_state.json`. Each new post's publish date is set to the actual run date, not a fixed date; confirmed again this run (20 July 2026 on all three new posts, both in the visible byline and the JSON-LD datePublished/dateModified). This has now been correct on every new post across eighteen consecutive runs (2-20 July). The June-16 date the client keeps flagging belongs only to 21 older published pages (front-loaded pillar/money pages and early authority posts from the 16-26 June period) that still carry a hardcoded "16 June 2026" (or other June) byline date predating this rule; this remains out of scope for this 3-post authority-only routine, which cannot fix it within its normal scope, and stays flagged as a standalone bulk backdate-correction job for the client to schedule separately (raised repeatedly now).
- Slack live-link format: every format tried so far (bare URL bundled in one message, bare URL in separate messages, markdown `[title](url)` bundled, URL as the literal last character of its own line either bundled or separate) has at some point been logged in BUILD-PLAN.md as a "confirmed fix" and then reported broken again by the client on a later run, so none of those prior claims should be trusted as settled. The client reported it broken again ahead of this run, so this run escalates to Slack's native `<url|display text>` link syntax, one article per separate `slack_send_message` call, since every bare-URL and markdown-link variant has now been tried and failed at least once. This is the first run of the native-syntax convention; verify against how the message actually renders in Slack before treating this as settled, and if it is reported broken again, the only remaining escalation is unformatted plain text with a forced line break before and after each URL and nothing else on that line.
- Watch items: (1) positioning-and-messaging-service (money page referenced by seq 423's up-link spec, not yet live; seq 422's up-link used sales-dashboards.html instead of the record's literal "crm-optimisation-service", which does not exist as a slug, matching the sibling post's live-page pattern), plus the standing backlog: salesforce-consultant, crm-automation-service (seq 96), sales-analytics-consulting (seq 104), uae-market-entry-service (seq 68), process-mapping-service (seq 85), crm-selection-service (seq 54), crm-consultant-for-startups (seq 122), crm-training-service (seq 123), b2b-gtm-strategy-service (seq 126), zoho-crm-implementation, customer-journey-mapping-service, hospitality-tech-consultant, sales-automation-service, go-to-market-strategy-consulting, product-launch-consulting, saas-gtm-strategy-service, gtm-strategy-uae, and the go-to-market pillar's remaining money pages are still not-started; this run's three posts link only to their live pillar/money pages since some specified target pages are not yet live. (2) link_graph.py reports the full graph clean (111 pages, 0 issues) as of this run; this run's three new posts were initially unlinked from elsewhere, fixed proactively before running link_graph.py by adding an inbound Related-reading link from two sibling pages each (defining-pipeline-stages.html + what-is-sales-pipeline-management.html; what-is-positioning.html + sales-playbook.html; what-is-a-good-conversion-rate.html + how-to-calculate-conversion-rate.html), so the graph reported clean on the first run. (3) This run's research used live web search (Validity's State of CRM Data Management in 2025 report, Salesforce's 2024 State of Sales report, Gartner's B2B buying journey research, IRP Commerce's ecommerce market data, Littledata's Shopify benchmark) rather than the Semrush MCP, which was not invoked this run; volumes and KD for seq 422-424 came from the existing plan.json records. (4) conversion-rate-formula.html and how-to-calculate-conversion-rate.html cover closely related queries (the formula itself vs. the step-by-step tracking method); deliberately differentiated in angle and cross-linked to each other rather than treated as duplicate coverage, worth a spot-check on a future audit pass.

## Fixed decisions
- Repo `Pearson26/pearson-co`; push to `main` via `gh` as `ngindubai` (accepted write collaborator).
- Static HTML, no build step. Netlify publish dir = repo root.
- Articles match the **live site** design (#B784A7, Playfair Display + DM Sans), not the agency-document look (#7f466f + Inter, which is for review documents only).
- Authority articles -> `/blog/<slug>.html`; pillar + money pages -> `/services/<slug>.html`. Root-relative asset paths.
- Byline author is Lauren Pearson, Founder.
- Barbell strategy: small set of money pages fed by a large body of authority content; pillar-and-cluster.
- 14 pillars, RevOps confirmed as a full pillar (umbrella adopted).

## Content rules (see CLAUDE.md for the full list)
- British English. No em dashes, ever. Banned words: delve, meticulous, comprehensive, leverage, seamless, robust (+ extended blocklist).
- Light scheme only. Headings use an italic last phrase via `<em>`.
- Every regulatory/statistical claim is sourced; no invented data or client names.

## Mistakes to avoid
- Do not push to a feature branch; deploy only fires on `main`.
- Do not modify index.html / styles.css / script.js during a content run (sitewide changes are a separate job).
- Do not pick work by date; progression is by pointer (`next_index`).
- Do not enable tracking IDs or wire Netlify without an explicit instruction.
