# Memory — The Pearson Co. content engine

Durable facts and current state. Update the Current State block in every content commit.

## Current state
- 2026-07-21: 114 posts live (next_index = 428). This run published seq 425-427: revenue-operations-team-structure, dynamics-365-implementation-guide (HowTo schema, up-link substitute crm-implementation-consultant.html since dynamics-crm-implementation is not yet live), fractional-cro-for-startups. All passed style gate, link_graph (114 pages, 0 issues), word-count floors with margin. Full detail in this run's BUILD-PLAN.md row and build_state.json notes.
- **Date-accuracy audit (closed out, non-issue):** the client's standing note that "articles are all published as June 16th" was investigated in full this run rather than repeated as background. Every file with a literal "16 June 2026" / "2026-06-16" date was found (exactly 21, all site/services/ pillar and money pages from the original launch) and cross-checked against its true git first-commit date: all 21 were genuinely first published on 16 June 2026 in one launch batch, so that date is accurate, not a bug, and must not be rewritten (doing so would falsify the record and risks a GSC date-manipulation flag). The 9 June-dated site/blog/ posts referenced in older logs already carry their own correct 27-29 June dates matching git history. Every new post this routine has ever published has always carried the correct real run date, confirmed again this run (21 July 2026 on all three). Treat this as resolved; if the client raises it again, point to this entry rather than re-auditing from scratch, unless new evidence of an actual live bug appears.
- **Slack link format (root cause likely found):** the `mcp__Slack__slack_send_message` tool's own description states it renders standard Markdown, including `[text](url)` links, not Slack's native `<url|text>` mrkdwn syntax. Last run (2026-07-20) used the native mrkdwn syntax, which is very likely what caused the reported bleed: a standard-Markdown renderer fed raw mrkdwn can leave the trailing `|Title>` unconsumed and merged into adjacent text. This run switched to `[Title](url)` standard Markdown links, one per line, matching the routine's literal "post one message, each article on its own line" instruction. Verify against actual Slack rendering before treating as fully settled. If it recurs, the only remaining escalation is a bare URL as the entire, sole content of its own message with nothing else in the string at all.
- Phase 1 (scaffold) complete. Repo seeded with the live site; engine in place.
- Phase 2 (the 12-month plan) COMPLETE: 1085 records across 14 pillars (15 pillar pages, 115 money, 955 authority) in `content-plan/plan.json`, rendered to `plan.md` + `plan-rows.js`, reviewable in `content-plan/content-plan.html`. Sequenced for publishing via `scripts/sequence_plan.py` (pillars + money front-loaded, authority round-robin; ~91/month).
- Volumes are live Semrush for batches 1-5 (Conversion, RevOps, CRM Implementation, Fractional+Growth, CJM part 1) and indicative (universe doc + domain knowledge) from batch 6 on. Refresh with Semrush when units are available, then re-run `render_plan.py`.
- 114 posts published: the 15 cornerstone pillar pages (seq 1-15), seq 16-39 (money pages across CRO, CRM, fractional, growth, market entry, ecommerce, RevOps pillars in site/services/), plus 75 authority posts in site/blog/, most recently seq 425-427 (revenue-operations-team-structure, dynamics-365-implementation-guide, fractional-cro-for-startups) published 2026-07-21. Full per-run seq history lives in BUILD-PLAN.md's session log. `next_index` = 428, routine resumes at seq 428.
- Netlify git continuous deployment is connected (push to main -> deploy). GA4 (G-3XWNSR5VHF) is live and consent-gated. Sitemap: https://thepearsonco.com/sitemap.xml (118 URLs, submit in GSC).
- 2026-07-20 SEO indexing fix (maintenance run, no posts). GSC flagged 42 URLs as "Discovered, currently not indexed" (Last crawled N/A). Diagnosed as NOT a sitemap fault: robots.txt, sitemap coverage, canonicals and meta robots are all correct, and there is no header-level noindex. Root cause is new-site crawl budget plus an internal-linking gap. The services hub (site/services/index.html) linked only 15 pillar services; added an "Every service, in one place" section linking all 39 service pages, so the long-tail money pages (previously 2-5 inbound links each) now have a strong inbound link from the hub. After deploy, request validation in GSC. The live site is not reachable from the build environment (org network policy blocks thepearsonco.com), so header checks were done from source only.
- The publishing routine (Strategist + 8 souls) is running: 3 posts/run, pointer-based from `build_state.json`. Each new post's publish date is set to the actual run date, not a fixed date; confirmed again this run (21 July 2026 on all three new posts, both in the visible byline and the JSON-LD datePublished/dateModified). Correct on every new post across nineteen consecutive runs (2-21 July). See the date-accuracy audit bullet above: the June-16 date question is now closed out as a non-issue, not an open backlog item.
- Watch items: (1) dynamics-crm-implementation (money page referenced by seq 426's up-link spec, not yet live; substituted crm-implementation-consultant.html, matching established precedent for missing up-link targets), plus the standing backlog: salesforce-consultant, crm-automation-service (seq 96), sales-analytics-consulting (seq 104), uae-market-entry-service (seq 68), process-mapping-service (seq 85), crm-selection-service (seq 54), crm-consultant-for-startups (seq 122), crm-training-service (seq 123), b2b-gtm-strategy-service (seq 126), zoho-crm-implementation, customer-journey-mapping-service, hospitality-tech-consultant, sales-automation-service, positioning-and-messaging-service, go-to-market-strategy-consulting, product-launch-consulting, saas-gtm-strategy-service, gtm-strategy-uae, and the go-to-market pillar's remaining money pages are still not-started; this run's three posts link only to live pillar/money pages since some specified targets are not yet live. (2) link_graph.py reports the full graph clean (114 pages, 0 issues) as of this run; this run's three new posts were initially unlinked from elsewhere, fixed proactively before running link_graph.py by adding an inbound Related-reading link from two sibling pages each, so the graph reported clean on the first run. (3) This run's research used live web search (Gartner's May 2021 RevOps press release, Microsoft's July 2025 Gartner Magic Quadrant recognition, MBO Partners' September 2025 State of Independence study) rather than the Semrush MCP, which was not invoked this run; volumes and KD for seq 425-427 came from the existing plan.json records.

## Fixed decisions
- Repo `Pearson26/pearson-co`; push to `main` via `gh` as `ngindubai` (accepted write collaborator).
- Static HTML, no build step. Netlify publish dir = repo root.
- Articles match the **live site** design (#B784A7, Playfair Display + DM Sans), not the agency-document look (#7f466f + Inter, which is for review documents only).
- Authority articles -> `/blog/<slug>.html`; pillar + money pages -> `/services/<slug>.html`. Root-relative asset paths.
- Byline author is Lauren Pearson, Founder.
- Barbell strategy: small set of money pages fed by a large body of authority content; pillar-and-cluster.
- 14 pillars, RevOps confirmed as a full pillar (umbrella adopted).

## Content rules (see CLAUDE.md for the full list)
- British English. No em dashes, ever. Banned words: delve, meticulous, comprehensive, leverage, seamless, robust (+ extended blocklist).
- Light scheme only. Headings use an italic last phrase via `<em>`.
- Every regulatory/statistical claim is sourced; no invented data or client names.

## Mistakes to avoid
- Do not push to a feature branch; deploy only fires on `main`.
- Do not modify index.html / styles.css / script.js during a content run (sitewide changes are a separate job).
- Do not pick work by date; progression is by pointer (`next_index`).
- Do not enable tracking IDs or wire Netlify without an explicit instruction.
